<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vinkas.library">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:process="@string/vinkas_id"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vinkas.Light.DarkActionBar">
        <activity android:name="vinkas.app.ConnectActivity"
            android:theme="@style/Theme.Vinkas.Light.NoActionBar" />
        <activity android:name="vinkas.app.GoogleConnectActivity"
            android:theme="@style/Theme.Vinkas.Light.NoActionBar" />
    </application>

</manifest>
